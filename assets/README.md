# Assets

This task uses generated tone WAV files under `assets/tones/`.

- Tones are simple sine-wave cues used for the left/right context sound.
- File names follow the pattern `tone_<ear>_<frequency>.wav`.
- The tone set is generated locally for the task and does not depend on external licensed media.

